#include "main.h"
/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
	int n = 8, len = 0;
	
	len = _printf("%b\n", n);
	_printf("%d chars been printed[_printf]\n", len);
	len = printf("%b\n", n);
	printf("%d chars been printed [printf]\n", len);
	n = 9;
	_printf("%b\n", n);
	n = 13;
	_printf("%b\n", n);
	n = 13247;
	_printf("%b\n", n);
	return (0);
}
